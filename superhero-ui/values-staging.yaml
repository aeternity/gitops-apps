app:
  image:
    repository: "aeternity/superhero-ui-web"
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "superhero.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "superhero-mega-test.dev.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
