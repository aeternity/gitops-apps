app:
  image:
    repository: "aeternity/superhero-ui-web"
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "superhero.dev.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "1168-merge.dev.aepps.com"
        paths:
          path: "/"
  versions: "1168-merge"
