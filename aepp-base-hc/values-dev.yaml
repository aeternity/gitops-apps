app:
  image:
    repository: aeternitybot
    tag: "master"
  ingress:
    enabled: true
    hosts:
      - host: "base-hc.dev.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
