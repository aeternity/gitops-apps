app:
  image:
    tag: "master"
  ingress:
    enabled: "true"
    hosts:
      - host: "base.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
  versions:
    - {version: pr-1442, gitSha: 3634f0a6}
