app:
  image:
    tag: "master"

  velero:
    enabled: "false"

  ingress:
    enabled: true
    hosts:
      - host: "base.dev.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
