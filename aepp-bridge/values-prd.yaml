app:
  ingress:
    hosts:
      - host: "bridge.prd.aepps.com"
        paths:
          - path: "/"
      - host: "bridge.aepps.com"
        paths:
          - path: "/"
      - host: "ae-bridge.com"
        paths:
          - path: "/"
      - host: "www.ae-bridge.com"
        paths:
          - path: "/"
    tls:
      - hosts:
          - "bridge.aepps.com"
          - "bridge.prd.aepps.com"
        secretName: aepp-bridge-cert
