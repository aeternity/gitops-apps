app:
  image:
    tag: develop
  resources:
    requests:
      memory: 24Mi
    limits:
      memory: 32Mi
  ingress:
    enabled: true
  secrets:
    external:
      values:
        VAPID_PRIVATE_KEY: /apps/BASE_VAPID_PRIVATE_KEY
