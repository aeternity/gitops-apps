app:
  image:
    tag: develop
  probes:
    liveness:
      enabled: false
    readiness:
      enabled: false
  ingress:
    enabled: true
  secrets:
    external:
      values:
        VAPID_PRIVATE_KEY: /apps/BASE_VAPID_PRIVATE_KEY
