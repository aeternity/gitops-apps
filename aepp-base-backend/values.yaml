app:
  image:
    tag: latest
  resources:
    requests:
      memory: 24Mi
    limits:
      memory: 32Mi
  service:
    port: 8079
  ingress:
    enabled: true
  secrets:
    external:
      values:
        VAPID_PRIVATE_KEY: /apps/BASE_VAPID_PRIVATE_KEY
