app:
  image:
    tag: develop
  resources:
    requests:
      cpu: 50m
      memory: 32Mi
    limits:
      cpu: 75m
      memory: 48Mi
  ingress:
    enabled: true
  secrets:
    external:
      values:
        VAPID_PRIVATE_KEY: /apps/BASE_VAPID_PRIVATE_KEY
