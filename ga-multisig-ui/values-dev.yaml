app:
  ingress:
    hosts:
      - host: "ga-multisig-ui.dev.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
        - "ga-multisig-ui.dev.aepps.com"
        secretName: ga-multisig-ui-cert
