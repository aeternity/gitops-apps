app:
  ingress:
    hosts:
      - host: "ga-multisig-ui.prd.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
        - "ga-multisig-ui.prd.aepps.com"
        secretName: ga-multisig-ui-cert
