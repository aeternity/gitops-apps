app:
  image:
    tag: "pr-82"
    pullPolicy: "Always"
  ingress:
    enabled: "true"
    hosts:
      - host: "develop-test-repo.dev.aepps.com"
        paths:
          - path: "/"
  probes:
    liveness:
      enabled: false
    readiness:
      enabled: false
