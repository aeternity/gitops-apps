app:
  image:
    repository: aeternitybot/test-repo
    pullPolicy: "Always"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.prd.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
            version: "main"
  versions:
    - {version: main, gitSha: 5db56f1}
