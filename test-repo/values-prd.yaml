app:
  image:
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.prd.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: latest-test-repo.prd.aepps.com
        paths:
          '- path': /
          '  version': latest
