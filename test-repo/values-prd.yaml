app:
  image:
    repository: aeternitybot/test-repo
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.prd.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
            version: "main"
  versions:
    - {version: main, gitSha: ""}
