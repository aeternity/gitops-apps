app:
  image:
    tag: "latest"
    pullPolicy: "Always"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "pr-71-test-repo.stg.aepps.com"
        paths:
          - path: "/"
            version: "pr-71"
    tls:
      - hosts: "pr-71-test-repo.stg.aepps.com"
  tls:
    - "test-repo.stg.aepps.com"
  versions:
    - version: "pr-71"
