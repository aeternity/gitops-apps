app:
  image:
    tag: "latest"
    pullPolicy: "Always"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "pr-45-test-repo.stg.aepps.com"
        paths:
          - path: "/"
            version: "pr-45"
  versions:
    - version: "pr-45"
