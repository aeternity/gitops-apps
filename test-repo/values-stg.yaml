app:
  image:
    tag: "latest"
    pullPolicy: "Always"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
    tls:
      - hosts:
        - "test-repo.stg.aepps.com"
  versions: []
