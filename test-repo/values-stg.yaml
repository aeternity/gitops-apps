app:
  image:
    tag: "latest"
    pullPolicy: "Always"
  ingress:
    enabled: "true"
    hosts:
      - host: "test-repo.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "pr-72-test-repo.stg.aepps.com"
        paths:
          - path: "/"
            version: "pr-72"
    tls:
      - hosts:
          - "test-repo.stg.aepps.com"
          - "pr-72-test-repo.stg.aepps.com"
        secretName: "test-repo-cert"
  versions:
    - version: "pr-72"
