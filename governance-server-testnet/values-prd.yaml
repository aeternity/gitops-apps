app:
  secrets:
    external:
      remotePrefix: /k8s/prd-0e9c
  ingress:
    hosts:
      - host: "governance-server-testnet.prd.aepps.com"
        paths:
          - path: "/"
  image:
    tag: governance-server-v1.0.1
