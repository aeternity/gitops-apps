apiVersion: v1
kind: Service
metadata:
  name: {{ include "aesophia.fullname" . }}
  labels:
    {{- include "aesophia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aesophia.selectorLabels" . | nindent 4 }}
