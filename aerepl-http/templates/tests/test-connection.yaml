apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aerepl_http.fullname" . }}-test-connection"
  labels:
    {{- include "aerepl_http.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aerepl_http.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
