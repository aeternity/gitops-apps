app:
  versions:
    - version: testnet
    - version: mainnet

  ingress:
    enabled: true
    hosts:
      - host: "explorer-testnet.prd.aepps.com" # has to be changed/disscused
        paths:
          - path: /
            version: "testnet"
      - host: "explorer-mainnet.prd.aepps.com" # has to be changed/disscused
        paths:
          - path: /
            version: "mainnet"
