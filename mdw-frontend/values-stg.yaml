app:
  versions:
    - testnet
    - mainnet

  ingress:
    enabled: true
    hosts:
      - host: "explorer-testnet.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: /
            version: "testnet"
      - host: "explorer-mainnet.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: /
            version: "mainnet"
