app:
  versions:
    - version: testnet
    - version: mainnet
  ingress:
    enabled: true
    hosts:
      - host: "explorer.dev.aepps.com"
        paths:
          - path: /
            version: "mainnet"
      - host: "explorer-testnet.dev.aepps.com" # has to be changed/discussed
        paths:
          - path: /
            version: "testnet"
