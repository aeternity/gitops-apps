app:
  versions:
    - version: testnet
    - version: mainnet

  velero:
    enabled: "false"
    
  ingress:
    enabled: true
    hosts:
      - host: "explorer-testnet.dev.aepps.com" # has to be changed/disscused
        paths:
          - path: /
            version: "testnet"
      - host: "explorer-mainnet.dev.aepps.com" # has to be changed/disscused
        paths:
          - path: /
            version: "mainnet"
