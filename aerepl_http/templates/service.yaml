apiVersion: v1
kind: Service
metadata:
  name: {{ include "aerepl_http.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aerepl_http.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aerepl_http.selectorLabels" . | nindent 4 }}
