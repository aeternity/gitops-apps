app:
  ingress:
    hosts:
      - host: "explorer-frontend.stg.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
        - "explorer-frontend.stg.aepps.com"
        secretName: explorer-frontend-cert
