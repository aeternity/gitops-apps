app:
  ingress:
    hosts:
      - host: "aescan-testnet.stg.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
        - "aescan-testnet.stg.aepps.com"
        secretName: aescan-testnet-cert
