app:
  secrets:
    external:
      remotePrefix: "/k8s/dev-wgt7"
  versions:
    - version: "master"
      gitSha: "edeee9f"
  ingress:
    hosts:
      - host: "aemon-testnet.dev.aepps.com"
        paths:
          - path: "/"
            version: "master"
