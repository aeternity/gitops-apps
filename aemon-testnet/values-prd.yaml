app:
  image:
    tag: v0.1.0
  ingress:
    hosts:
      - host: "aemon-testnet.prd.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
          - "aemon-testnet.prd.aepps.com"
        secretName: aemon-testnet-cert
