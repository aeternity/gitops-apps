app:
  resources:
    limits:
      memory: 384Mi
    requests:
      memory: 384Mi
  ingress:
    hosts:
      - host: "mdw-explorer-testnet.prd.aepps.com"
        paths:
          - path: /
    tls:
      - hosts:
        - "mdw-explorer-testnet.prd.aepps.com"
        secretName: mdw-frontend-testnet-cert
