app:
  image:
    repository: "aeternity/superhero-ui-web"
    tag: "latest"
    pullPolicy: Always
  ingress:
    enabled: "true"
    hosts:
      - host: "superhero-ssr.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
  versions: []
