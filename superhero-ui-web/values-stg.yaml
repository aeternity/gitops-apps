app:
  image:
    repository: "aeternity/superhero-ui-ssr"
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "superhero-ssr.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
