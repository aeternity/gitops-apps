app:
  ingress:
    hosts:
      - host: "state-channel-demo-backend.dev.aepps.com"
        paths:
          - path: "/"
      - host: "develop-state-channel-demo-backend.dev.aepps.com"
        paths:
          - path: "/"
            version: "develop"
  versions:
    - {version: "develop", gitSha: "55d4713"}
  env:
    "NODE_ENV": "production"
