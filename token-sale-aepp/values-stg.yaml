app:
  ingress:
    hosts:
      - host: "token-sale.stg.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
        - "token-sale.stg.aepps.com"
        secretName: token-sale-cert
