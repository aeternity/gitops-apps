app:
  image:
    repository: "aeternity/superhero-wallet"
    tag: "latest"
    pullPolicy: Always
  ingress:
    enabled: "true"
    hosts:
      - host: "superhero-wallet.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
  versions:
    - {version: pr-1412, gitSha: 091d53e9}
