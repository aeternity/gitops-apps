app:
  image:
    tag: "v1.17.1"
  ingress:
    enabled: "true"
    hosts:
      - host: "dex.prd.aepps.com"
        paths:
          - path: "/"
      - host: "aepp.dex.superhero.com"
        paths:
          - path: "/"
      - host: "main-dex-ui.prd.aepps.com"
        paths:
          - path: "/"
            version: "main"
  versions:
    - {version: main, gitSha: 23bcfa9}
