app:
  image:
    tag: "v1.8.0"
  ingress:
    enabled: "true"
    hosts:
      - host: "dex.prd.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "aepp.dex.superhero.com" # has to be changed/disscused
        paths:
          - path: "/"
