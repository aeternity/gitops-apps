app:
  image:
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "dex.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "pr-112-dex-ui.stg.aepps.com"
        paths:
          - path: "/"
            version: "pr-112"
  versions:
    - {version: pr-112, gitSha: 011c4b2}
