app:
  image:
    tag: "latest"
  ingress:
    enabled: "true"
    hosts:
      - host: "dex.stg.aepps.com" # has to be changed/disscused
        paths:
          - path: "/"
      - host: "pr-36-dex-ui.stg.aepps.com"
        paths:
          - path: "/"
  versions:
    - "pr-36"
